version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_TZ=Asia/Kolkata
      - DATABASE_URL=sqlite:///./backend/bookings.db
    volumes:
      - ./:/app
